apiVersion: v1
kind: Service
metadata:
  name: {{ include "shows.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "shows.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
